# Environment Configuration Example
# Copy this to .env and adjust values as needed

# Database URL
# Use sqlite::memory: for in-memory database (no persistence)
# Use sqlite:path/to/file.db for file-backed database
DB_URL=sqlite::memory:

# Uncomment to use a file-backed database
# DB_URL=sqlite:shop_demo.db

# Application log level (if logging is added)
# RUST_LOG=info
